{/*'use client';

import { motion } from 'framer-motion';
//import { timeline } from '@/lib/content';
import SectionLabel from './ui/SectionLabel';

export default function Timeline() {
  return (
    <section id="timeline" className="relative bg-navy px-6 py-28 md:px-12">
      <div className="mx-auto max-w-4xl">
        <SectionLabel align="center">The Years</SectionLabel>
        <h2 className="mx-auto mt-4 max-w-xl text-center font-display text-3xl text-frost sm:text-4xl">
          A life, in a handful of moments
        </h2>
      </div>

      <div className="relative mx-auto mt-20 max-w-3xl">
        {/* Spine 
        <div className="absolute left-4 top-0 h-full w-px bg-gradient-to-b from-transparent via-gold-dim to-transparent md:left-1/2 md:-translate-x-1/2" />

        <ol className="space-y-16">
          {timeline.map((item, i) => {
            const isRight = i % 2 === 1;
            return (
              <li key={item.id} className="relative">
                {/* Marker dot 
                <motion.span
                  initial={{ scale: 0 }}
                  whileInView={{ scale: 1 }}
                  viewport={{ once: true }}
                  transition={{ duration: 0.4, delay: 0.1 }}
                  className="absolute left-4 top-2 z-10 h-2.5 w-2.5 -translate-x-1/2 rounded-full bg-gold shadow-gold md:left-1/2"
                />

                <motion.div
                  initial={{ opacity: 0, x: isRight ? 40 : -40 }}
                  whileInView={{ opacity: 1, x: 0 }}
                  viewport={{ once: true, margin: '-100px' }}
                  transition={{ duration: 0.7, ease: [0.22, 1, 0.36, 1] }}
                  className={`glass ml-10 grid grid-cols-1 gap-4 rounded-2xl p-5 sm:grid-cols-[120px_1fr] md:ml-0 md:w-[calc(50%-2.5rem)] ${
                    isRight ? 'md:ml-auto' : ''
                  }`}
                >
                  <div className="overflow-hidden rounded-lg">
                    {/* Replace with personal photo 
                    <img
                      src={item.src}
                      alt={item.title}
                      loading="lazy"
                      className="photo-duotone h-28 w-full object-cover sm:h-full"
                    />
                  </div>
                  <div>
                    <span className="label-eyebrow">{item.year}</span>
                    <h3 className="mt-1 font-display text-lg text-frost">{item.title}</h3>
                    <p className="mt-2 text-sm leading-relaxed text-frost-dim">{item.text}</p>
                  </div>
                </motion.div>
              </li>
            );
          })}
        </ol>
      </div>
    </section>
  );
}
*/}
